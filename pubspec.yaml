name: apicli
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  json_annotation:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  chopper: ^3.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.7.3
  chopper_generator: ^3.0.1+1
flutter:
  uses-material-design: true

